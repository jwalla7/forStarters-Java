package ADT.Trees.BinaryTrees;

/*
*   LevelOrder Recursive Pseudocode (Uses a Queue Data Structure)
 *   levelorder() {
 *      Create Queue q
 *      Add root to q
 *      while(q is not empty) {
 *          Node curr = q.dequeue()
 *          if (curr is not null) {
 *              q.enqueue(curr.left)
 *              q.enqueue(curr.right)
 *          }
 *      }
*    }
*/

public class LevelOrder {
}
